import React from 'react';
import { View } from 'react-native';

export default SheetHeader = () => {
    return (
        <View style={{ flex: 1, backgroundColor: "#fff", padding: 10, justifyContent: 'center', alignItems: 'center', }}>
        <View style={{ width: 30, height: 5, backgroundColor: "#000", borderRadius: 5 }} />
    </View>
    )
}
